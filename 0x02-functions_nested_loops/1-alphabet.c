#include "main.h"
/**
 * print_alphabet - function to print alphabet in lower case
 * Return: 0 always
 */

void print_alphabet(void)
{
	char i;

	for (i = 'a' ; i <= 10 ; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
